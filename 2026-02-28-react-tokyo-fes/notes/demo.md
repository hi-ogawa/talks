# Demo preparation notes

## TODO

- runnable code blocks examples from poster
  - on stackblitz?
  - this can be also deployed with returning `util.format` from server?
- deployed example with `use server` (`encodeReply` demo)
- deployed example with actual `"use cache"`
  - @vitejs/plugin-rsc
  - waku
  - react-router
  - vite-plugin-react-use-cache

## Code block examples

```
```
