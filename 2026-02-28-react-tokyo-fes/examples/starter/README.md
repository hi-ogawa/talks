# Demo

```sh
pnpm -C examples/starter vite-run src/demo.tsx

# run with production to see clean structure React's debug metadata
NODE_ENV=production pnpm -C examples/starter vite-run src/demo.tsx
```
